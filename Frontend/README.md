# E.V. Frontend

React, Vite, Tailwind CSS, Framer Motion, Lucide React, and Axios frontend for E.V.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run preview
```

Set `VITE_API_BASE_URL` only when the backend is hosted on a different origin.
